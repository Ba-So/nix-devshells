# Iteration Loop

Task: $ARGUMENTS

```
CODE ──► REVIEW ──► APPROVED ──► DONE
           │
           └─► FIX ──► REVIEW (max 3×, then BLOCK)
```

Track iteration count per task. Block and report after 3 failed reviews.
